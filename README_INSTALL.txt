# Summer Breeze AI Web Panel

## Installation Steps:
1. Upload all files (`index.html`, `panel.js`, `listener.js`) to your GitHub Pages repo.
2. Make sure GitHub Pages is enabled and pointing to the root directory of the `main` branch.
3. Visit your GitHub Pages URL (e.g. `https://username.github.io/repo-name/`).
4. Click "Speak" or type messages — future backend integration can process replies.

This panel is designed to connect to a backend LLM processor (like MindPal, OpenAI, or Ollama).
